services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: studycoach
      POSTGRES_PASSWORD: studycoach
      POSTGRES_DB: studycoach_development
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: